# Etapa única: entorno de desarrollo
FROM node:20-slim

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

# Expone el puerto de desarrollo asignado
EXPOSE 5177

CMD ["npx", "vite", "--host", "0.0.0.0", "--port", "5177"]